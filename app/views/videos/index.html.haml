%article.video_category
  %header
    %h3= link_to "Category Holder"
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to video_path(video), nil do
          %img(src="/tmp/#{video.cover_image_url}")

